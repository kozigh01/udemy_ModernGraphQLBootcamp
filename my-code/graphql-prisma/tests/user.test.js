import { getFirstName } from '../src/utils/user.js';

test('should return first name when given full name', () => {
    const fname = getFirstName('First Name');
});

test('this is my first test case', () => {
    1 + 2;
});

test('my first test challenge', () => {
    'hello world'
});